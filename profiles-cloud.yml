default:
  target: dev
  outputs:
    dev:
      type: clickhouse
      schema: "{{ DBT_SCHEMA }}"
      host: "{{ DBT_HOST }}"
      port: 8443
      user: "{{ DBT_USER }}"
      password: "{{ DBT_PASSWORD }}"
      secure: True
      custom_settings:
        select_sequential_consistency: 1